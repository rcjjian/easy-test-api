#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//usr/bin/node" ]; then
  "$basedir//usr/bin/node"  "$basedir/../acquit-require/bin/acquit-require.js" "$@"
  ret=$?
else 
  /usr/bin/node  "$basedir/../acquit-require/bin/acquit-require.js" "$@"
  ret=$?
fi
exit $ret
